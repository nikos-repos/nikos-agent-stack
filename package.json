{
  "name": "nikos-agent-stack",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test gate-checker/*.test.ts && bun run gate-checker/index.ts && bun run gate-checker/wiring-check.ts"
  }
}

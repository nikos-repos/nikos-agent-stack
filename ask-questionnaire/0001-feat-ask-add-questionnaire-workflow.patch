From 0613b2d898d082bfdde979160af30c4775053f85 Mon Sep 17 00:00:00 2001
From: nikos-repos <nikobono43@gmail.com>
Date: Wed, 12 Aug 2026 19:57:03 -0500
Subject: [PATCH] feat(ask): add questionnaire workflow

---
 packages/agent/src/agent.ts                   |  37 +-
 .../src/modes/components/ask-dialog.ts        | 185 ++++++---
 .../src/modes/components/ask-inline-input.ts  |  35 ++
 .../controllers/extension-ui-controller.ts    |  48 ++-
 .../coding-agent/src/prompts/tools/ask.md     |   3 +-
 .../coding-agent/src/session/agent-session.ts |  99 ++++-
 packages/coding-agent/src/tools/ask.ts        |  17 +-
 .../coding-agent/src/utils/external-editor.ts |  47 ++-
 .../agent-session-force-tool-choice.test.ts   | 183 ++++++++-
 .../test/collab/guest-ui-request.test.ts      |  28 +-
 .../coding-agent/test/external-editor.test.ts |  61 +++
 .../test/modes/components/ask-dialog.test.ts  | 386 ++++++++++--------
 packages/coding-agent/test/tools/ask.test.ts  |  65 +--
 13 files changed, 851 insertions(+), 343 deletions(-)
 create mode 100644 packages/coding-agent/src/modes/components/ask-inline-input.ts

diff --git a/packages/agent/src/agent.ts b/packages/agent/src/agent.ts
index c2e779a7e2..3488c9ac4b 100644
--- a/packages/agent/src/agent.ts
+++ b/packages/agent/src/agent.ts
@@ -426,7 +426,9 @@ export class Agent {
 	#asideMessageProvider?: () => AsideMessage[] | Promise<AsideMessage[]>;
 	#telemetry?: AgentLoopConfig["telemetry"];
 	#appendOnlyContext?: AppendOnlyContextManager;
-	#beforeQueuedMessageDequeueHooks = new Set<(signal?: AbortSignal) => Promise<void> | void>();
+	#beforeQueuedMessageDequeueHooks = new Set<
+		(signal?: AbortSignal, kind?: "steer" | "followUp") => boolean | void | Promise<boolean | void>
+	>();
 	#beforeModelCallHooks = new Set<(signal?: AbortSignal) => Promise<void> | void>();
 
 	/** Buffered Cursor tool results with text length at time of call (for correct ordering) */
@@ -786,9 +788,12 @@ export class Agent {
 		return () => this.#listeners.delete(fn);
 	}
 
-	/** Register an independently removable hook that runs before queued messages are consumed. */
-	addBeforeQueuedMessageDequeueHook(hook: (signal?: AbortSignal) => Promise<void> | void): () => void {
-		const registration = (signal?: AbortSignal) => hook(signal);
+	/** register a removable hook before a specific queued-message kind is consumed.
+	 * return true to limit this dequeue to the queue head. */
+	addBeforeQueuedMessageDequeueHook(
+		hook: (signal?: AbortSignal, kind?: "steer" | "followUp") => boolean | void | Promise<boolean | void>,
+	): () => void {
+		const registration = (signal?: AbortSignal, kind?: "steer" | "followUp") => hook(signal, kind);
 		this.#beforeQueuedMessageDequeueHooks.add(registration);
 		return () => this.#beforeQueuedMessageDequeueHooks.delete(registration);
 	}
@@ -804,20 +809,24 @@ export class Agent {
 		for (const hook of this.#beforeModelCallHooks) await hook(signal);
 	}
 
-	async #runBeforeQueuedMessageDequeueHooks(signal?: AbortSignal): Promise<void> {
-		for (const hook of this.#beforeQueuedMessageDequeueHooks) await hook(signal);
+	async #runBeforeQueuedMessageDequeueHooks(kind: "steer" | "followUp", signal?: AbortSignal): Promise<boolean> {
+		let dequeueHeadOnly = false;
+		for (const hook of this.#beforeQueuedMessageDequeueHooks) {
+			if ((await hook(signal, kind)) === true) dequeueHeadOnly = true;
+		}
+		return dequeueHeadOnly;
 	}
 
 	async #dequeueSteeringMessagesAfterHooks(signal?: AbortSignal): Promise<AgentMessage[]> {
 		if (signal?.aborted || this.#steeringQueue.length === 0) return [];
-		await this.#runBeforeQueuedMessageDequeueHooks(signal);
-		return signal?.aborted ? [] : this.#dequeueSteeringMessages();
+		const dequeueHeadOnly = await this.#runBeforeQueuedMessageDequeueHooks("steer", signal);
+		return signal?.aborted ? [] : this.#dequeueSteeringMessages(dequeueHeadOnly);
 	}
 
 	async #dequeueFollowUpMessagesAfterHooks(signal?: AbortSignal): Promise<AgentMessage[]> {
 		if (signal?.aborted || this.#followUpQueue.length === 0) return [];
-		await this.#runBeforeQueuedMessageDequeueHooks(signal);
-		return signal?.aborted ? [] : this.#dequeueFollowUpMessages();
+		const dequeueHeadOnly = await this.#runBeforeQueuedMessageDequeueHooks("followUp", signal);
+		return signal?.aborted ? [] : this.#dequeueFollowUpMessages(dequeueHeadOnly);
 	}
 
 	setProviderResponseInterceptor(fn: SimpleStreamOptions["onResponse"] | undefined): void {
@@ -1030,8 +1039,8 @@ export class Agent {
 		return this.#abortController?.signal.aborted === true && this.#state.isStreaming;
 	}
 
-	#dequeueSteeringMessages(): AgentMessage[] {
-		if (this.#steeringMode === "one-at-a-time") {
+	#dequeueSteeringMessages(dequeueHeadOnly = false): AgentMessage[] {
+		if (dequeueHeadOnly || this.#steeringMode === "one-at-a-time") {
 			if (this.#steeringQueue.length > 0) {
 				const first = this.#steeringQueue[0];
 				this.#steeringQueue = this.#steeringQueue.slice(1);
@@ -1044,8 +1053,8 @@ export class Agent {
 		return steering;
 	}
 
-	#dequeueFollowUpMessages(): AgentMessage[] {
-		if (this.#followUpMode === "one-at-a-time") {
+	#dequeueFollowUpMessages(dequeueHeadOnly = false): AgentMessage[] {
+		if (dequeueHeadOnly || this.#followUpMode === "one-at-a-time") {
 			if (this.#followUpQueue.length > 0) {
 				const first = this.#followUpQueue[0];
 				this.#followUpQueue = this.#followUpQueue.slice(1);
diff --git a/packages/coding-agent/src/modes/components/ask-dialog.ts b/packages/coding-agent/src/modes/components/ask-dialog.ts
index 80ed90e3c2..e060749f20 100644
--- a/packages/coding-agent/src/modes/components/ask-dialog.ts
+++ b/packages/coding-agent/src/modes/components/ask-dialog.ts
@@ -1,5 +1,6 @@
 import {
 	type Component,
+	Editor,
 	Ellipsis,
 	Markdown,
 	type MarkdownTheme,
@@ -12,6 +13,7 @@ import {
 	Text,
 	type TUI,
 	truncateToWidth,
+	visibleWidth,
 	wrapTextWithAnsi,
 } from "@oh-my-pi/pi-tui";
 import type {
@@ -21,20 +23,23 @@ import type {
 	ExtensionAskDialogSubmitResult,
 } from "../../extensibility/extensions";
 import { getTabBarTheme } from "../shared";
-import { getMarkdownTheme, highlightCode, theme } from "../theme/theme";
+import { getEditorTheme, getMarkdownTheme, highlightCode, theme } from "../theme/theme";
 import {
+	matchesAppExternalEditor,
 	matchesSelectCancel,
 	matchesSelectDown,
 	matchesSelectPageDown,
 	matchesSelectPageUp,
 	matchesSelectUp,
 } from "../utils/keybinding-matchers";
+import { renderAskInlineInputRow } from "./ask-inline-input";
 import { CountdownTimer } from "./countdown-timer";
 import { editorKey } from "./keybinding-hints";
 import { bottomBorder, divider, row, topBorder } from "./overlay-box";
 import { handleTabSwitchKey } from "./selector-helpers";
 
-const OTHER_OPTION = "Other (type your own)";
+const OTHER_OPTION = "Type something.";
+const NEXT_OPTION = "Next";
 const SUBMIT_OPTION = "Submit";
 
 /** Fraction of the terminal the dialog may occupy. The box height is fixed
@@ -78,6 +83,7 @@ interface AskDialogCallbacks {
 	onSubmit(result: ExtensionAskDialogSubmitResult): void;
 	onCancel(): void;
 	onPrompt(title: string, prefill?: string): Promise<string | undefined>;
+	onEditInput?(value: string): Promise<string | undefined>;
 }
 
 interface AskDialogInputGuard {
@@ -106,9 +112,10 @@ interface QuestionState {
 	scrollOffset: number;
 	manualScroll: boolean;
 	timedOut: boolean;
+	inlineInput: Editor;
 }
 
-type QuestionRowKind = "option" | "other";
+type QuestionRowKind = "option" | "other" | "next";
 
 interface QuestionRow {
 	kind: QuestionRowKind;
@@ -296,6 +303,14 @@ function optionMarker(question: ExtensionAskDialogQuestion, checked: boolean): s
 	return checked ? theme.radio.selected : theme.radio.unselected;
 }
 
+function editorCursorOffset(editor: Editor): number {
+	const cursor = editor.getCursor();
+	const lines = editor.getLines();
+	let offset = 0;
+	for (let index = 0; index < cursor.line; index++) offset += (lines[index]?.length ?? 0) + 1;
+	return offset + cursor.col;
+}
+
 function renderRowLabel(
 	rowItem: QuestionRow,
 	question: ExtensionAskDialogQuestion,
@@ -307,20 +322,35 @@ function renderRowLabel(
 ): string[] {
 	const isOption = rowItem.kind === "option";
 	const isOther = rowItem.kind === "other";
+	const isNext = rowItem.kind === "next";
 	const checked = isOption
 		? state.selectedOptions.has(stripRecommendedSuffix(rowItem.label))
 		: isOther && state.customInput !== undefined;
 	const color = selected ? "accent" : checked ? "toolOutput" : "text";
-	const marker = `${theme.fg(checked ? "success" : "dim", optionMarker(question, checked))} `;
+	const marker = isNext ? "  " : `${theme.fg(checked ? "success" : "dim", optionMarker(question, checked))} `;
 	const cursor = selected ? theme.fg("accent", `${theme.nav.cursor} `) : "  ";
-	const label = renderInlineMarkdown(rowItem.label, mdTheme, t => theme.fg(color, t));
 	const noteMarker = state.note && state.noteRowKey === rowItem.key ? theme.fg("success", "  ✎ note") : "";
+	if (isOther && selected) {
+		const prefix = `${cursor}${marker}`;
+		const continuationPrefix = " ".repeat(visibleWidth(prefix));
+		return renderAskInlineInputRow({
+			buffer: state.inlineInput.getText(),
+			cursorOffset: editorCursorOffset(state.inlineInput),
+			rowPrefix: prefix,
+			continuationPrefix,
+			contentWidth: Math.max(1, width - visibleWidth(prefix)),
+			selectedText: text => theme.bold(theme.fg("accent", text)),
+		});
+	}
+	const draft = isOther ? state.inlineInput.getText() : "";
+	const displayLabel = isOther && draft.length > 0 ? replaceTabs(draft).replace(/\s+/g, " ") : rowItem.label;
+	const label = renderInlineMarkdown(displayLabel, mdTheme, text => theme.fg(color, text));
 	const firstLine = `${cursor}${marker}${label}${noteMarker}`;
 	const lines = [truncateToWidth(firstLine, width, Ellipsis.Unicode)];
 	if (rowItem.kind === "option") {
 		const option = question.options[rowItem.optionIndex ?? -1];
 		if (option?.description?.trim()) {
-			const description = renderInlineMarkdown(option.description.trim(), mdTheme, t => theme.fg("muted", t));
+			const description = renderInlineMarkdown(option.description.trim(), mdTheme, text => theme.fg("muted", text));
 			const wrapped = wrapTextWithAnsi(description, Math.max(1, width - 6));
 			for (const line of wrapped.slice(0, 2)) {
 				lines.push(`      ${truncateToWidth(line, Math.max(1, width - 6), Ellipsis.Unicode)}`);
@@ -331,10 +361,6 @@ function renderRowLabel(
 			lines.push(...renderCachedPreview(previewCache, option.preview, previewWidth));
 		}
 	}
-	if (isOther && state.customInput !== undefined) {
-		const preview = replaceTabs(state.customInput).replace(/\s+/g, " ").trim();
-		lines.push(theme.fg("muted", `      ${truncateToWidth(preview, Math.max(1, width - 6), Ellipsis.Unicode)}`));
-	}
 	return lines;
 }
 
@@ -386,8 +412,10 @@ export class AskDialogComponent implements Component {
 	#remainingSeconds: number | undefined;
 	#countdown: CountdownTimer | undefined;
 	#promptActive = false;
+	#inputEditorOpen = false;
 	#timeoutExpired = false;
 	#closed = false;
+	#collapsed = false;
 	#tabBar: TabBar | undefined;
 	#stableHeight: { key: string; total: number } | undefined;
 	#previewCache: PreviewRenderCache = new Map();
@@ -412,6 +440,7 @@ export class AskDialogComponent implements Component {
 				scrollOffset: 0,
 				manualScroll: false,
 				timedOut: false,
+				inlineInput: new Editor(getEditorTheme()),
 			};
 		});
 		if (options.timeout && options.timeout > 0) {
@@ -439,10 +468,21 @@ export class AskDialogComponent implements Component {
 	}
 
 	handleInput(keyData: string): void {
-		if (this.#closed || this.#promptActive) return;
-		// Reset the inactivity countdown on any key that reaches past the
-		// closed/prompt guards, matching HookSelector/HookInput semantics.
+		if (this.#closed || this.#promptActive || this.#inputEditorOpen) return;
 		this.#countdown?.reset();
+		if (matchesKey(keyData, "ctrl+]")) {
+			this.#collapsed = !this.#collapsed;
+			this.#requestRender();
+			return;
+		}
+		if (this.#collapsed) {
+			if (matchesSelectCancel(keyData)) this.#finishCancel();
+			return;
+		}
+		if (this.#isInlineInputActive() && matchesAppExternalEditor(keyData)) {
+			this.#handleQuestionInput(keyData);
+			return;
+		}
 		if (matchesSelectCancel(keyData)) {
 			this.#finishCancel();
 			return;
@@ -453,6 +493,10 @@ export class AskDialogComponent implements Component {
 			this.#requestRender();
 			return;
 		}
+		if (this.#isInlineInputActive()) {
+			this.#handleQuestionInput(keyData);
+			return;
+		}
 		if (this.#hasSubmitTab() && handleTabSwitchKey(keyData, direction => this.#switchTab(direction))) {
 			this.#requestRender();
 			return;
@@ -469,6 +513,7 @@ export class AskDialogComponent implements Component {
 		// renders as the next sibling in the same container, so this lands in the
 		// same frame).
 		this.options.inputGuard?.syncPresentation?.();
+		if (this.#collapsed) return [theme.fg("dim", " ctrl+] to expand · esc to cancel ")];
 		const innerWidth = Math.max(1, width - 4);
 		// Fixed panel height: measured from the tallest tab at spawn and
 		// re-measured only when the viewport changes. Tab switches, cursor
@@ -599,16 +644,17 @@ export class AskDialogComponent implements Component {
 		if (inputGuard?.isBlocked()) return `${inputGuard.hint} · ${cancel}`;
 		if (this.#isSubmitTab()) {
 			const scroll = indicator ? ` ${indicator} scroll ·` : "";
-			return `Enter submit · ↑/↓ scroll ·${scroll} ${cancel}`;
+			return `Enter submit · ↑/↓${scroll} · Ctrl+] · ${cancel}`;
 		}
-		const question = this.#questions[this.#currentQuestionIndex()];
-		const action = question?.multi ? "Space/Enter toggle · n note" : "Enter select · n note";
-		const tabs = this.#hasSubmitTab() ? " · Tab/←/→" : "";
-		if (this.#questionCanPage && indicator) {
-			return `${action} · ↑/↓${tabs} · ${cancel} · ${pageKeysLabel()} ${indicator}`;
+		if (this.#isInlineInputActive()) {
+			return `Enter submit · Shift+Enter new line · Ctrl+U clear · Ctrl+G edit · Ctrl+] · ${cancel}`;
 		}
-		const scroll = indicator ? ` ${indicator} scroll ·` : "";
-		return `${action} · ↑/↓ move${tabs} ·${scroll} ${cancel}`;
+		const question = this.#questions[this.#currentQuestionIndex()];
+		const action = question?.multi ? "Space toggle · n note" : "Enter select · n note";
+		const tabs = this.#hasSubmitTab() ? " · Tab" : "";
+		const page = this.#questionCanPage && indicator ? ` · ${pageKeysLabel()} ${indicator}` : "";
+		const scroll = !this.#questionCanPage && indicator ? ` ${indicator} scroll` : "";
+		return `${action} · ↑/↓${scroll}${page}${tabs} · Ctrl+] · ${cancel}`;
 	}
 
 	#questionRows(question: ExtensionAskDialogQuestion): QuestionRow[] {
@@ -619,6 +665,7 @@ export class AskDialogComponent implements Component {
 			optionIndex: index,
 		}));
 		rows.push({ kind: "other", key: "other", label: OTHER_OPTION, optionIndex: undefined });
+		if (question.multi) rows.push({ kind: "next", key: "next", label: NEXT_OPTION, optionIndex: undefined });
 		return rows;
 	}
 
@@ -633,11 +680,54 @@ export class AskDialogComponent implements Component {
 		return { question, state };
 	}
 
+	#isInlineInputActive(): boolean {
+		if (this.#isSubmitTab()) return false;
+		const active = this.#activeQuestionState();
+		if (!active) return false;
+		return this.#questionRows(active.question)[active.state.cursorIndex]?.kind === "other";
+	}
+
 	#handleQuestionInput(keyData: string): void {
 		const active = this.#activeQuestionState();
 		if (!active) return;
 		const { question, state } = active;
 		const rows = this.#questionRows(question);
+		const rowItem = rows[state.cursorIndex];
+		if (!rowItem) return;
+		if (rowItem.kind === "other") {
+			const isEnter = matchesKey(keyData, "enter") || matchesKey(keyData, "return") || keyData === "\n";
+			if (matchesAppExternalEditor(keyData)) {
+				void this.#editInlineInput(state);
+				return;
+			}
+			if (isEnter) {
+				state.customInput = state.inlineInput.getText();
+				state.selectedOptions.clear();
+				clearNoteUnlessRow(state, rowItem.key);
+				this.#advanceAfterQuestion();
+				return;
+			}
+			if (matchesKey(keyData, "tab")) return;
+			const cursor = state.inlineInput.getCursor();
+			const lastLine = state.inlineInput.getLines().length - 1;
+			if (matchesSelectUp(keyData) && cursor.line === 0) {
+				state.cursorIndex = (state.cursorIndex - 1 + rows.length) % rows.length;
+				state.manualScroll = false;
+				this.#requestRender();
+				return;
+			}
+			if (matchesSelectDown(keyData) && cursor.line === lastLine) {
+				state.cursorIndex = (state.cursorIndex + 1) % rows.length;
+				state.manualScroll = false;
+				this.#requestRender();
+				return;
+			}
+			state.inlineInput.handleInput(keyData);
+			state.customInput = undefined;
+			clearNoteIfRow(state, rowItem.key);
+			this.#requestRender();
+			return;
+		}
 		if (matchesSelectPageUp(keyData)) {
 			state.scrollOffset = Math.max(0, state.scrollOffset - Math.max(1, this.#bodyRows - 1));
 			state.manualScroll = true;
@@ -651,37 +741,32 @@ export class AskDialogComponent implements Component {
 			return;
 		}
 		if (matchesSelectUp(keyData)) {
-			state.cursorIndex = clamp(state.cursorIndex - 1, 0, Math.max(0, rows.length - 1));
+			state.cursorIndex = (state.cursorIndex - 1 + rows.length) % rows.length;
 			state.manualScroll = false;
 			this.#requestRender();
 			return;
 		}
 		if (matchesSelectDown(keyData)) {
-			state.cursorIndex = clamp(state.cursorIndex + 1, 0, Math.max(0, rows.length - 1));
+			state.cursorIndex = (state.cursorIndex + 1) % rows.length;
 			state.manualScroll = false;
 			this.#requestRender();
 			return;
 		}
-		const rowItem = rows[state.cursorIndex];
-		if (!rowItem) return;
 		if (keyData === "n" || keyData === "N") {
-			if (rowItem.kind === "option" || rowItem.kind === "other") {
-				void this.#promptForNote(question, state, rowItem);
-			}
+			if (rowItem.kind === "option") void this.#promptForNote(question, state, rowItem);
 			return;
 		}
 		const isEnter = matchesKey(keyData, "enter") || matchesKey(keyData, "return") || keyData === "\n";
 		const isSpace = matchesKey(keyData, "space") || keyData === " ";
 		if (!isEnter && !(question.multi && isSpace)) return;
-		if (rowItem.kind === "other") {
-			void this.#promptForCustomInput(question, state, rowItem);
+		if (rowItem.kind === "next") {
+			if (isEnter) this.#advanceAfterQuestion();
 			return;
 		}
 		const option = question.options[rowItem.optionIndex ?? -1];
 		if (!option) return;
 		if (question.multi) {
-			// Multi is toggle-only: Enter and Space both toggle, and the
-			// answer is confirmed from the Submit tab.
+			state.customInput = undefined;
 			if (state.selectedOptions.has(option.label)) {
 				state.selectedOptions.delete(option.label);
 				clearNoteIfRow(state, rowItem.key);
@@ -730,34 +815,22 @@ export class AskDialogComponent implements Component {
 		this.#requestRender();
 	}
 
-	async #promptForCustomInput(
-		question: ExtensionAskDialogQuestion,
-		state: QuestionState,
-		rowItem: QuestionRow,
-	): Promise<void> {
-		this.#promptActive = true;
+	async #editInlineInput(state: QuestionState): Promise<void> {
+		const editInput = this.callbacks.onEditInput;
+		if (!editInput || this.#inputEditorOpen) return;
+		this.#inputEditorOpen = true;
 		try {
-			const input = await this.callbacks.onPrompt(
-				boundPromptTitle("Custom answer: ", question.question),
-				state.customInput,
-			);
-			if (input === undefined || this.#closed) return;
-			if (input.trim() === "") {
-				// Submitting an empty value unselects the custom answer.
-				state.customInput = undefined;
-				clearNoteIfRow(state, rowItem.key);
-				return;
-			}
-			state.customInput = input;
-			if (!question.multi) {
+			const value = await editInput(state.inlineInput.getText());
+			if (value !== undefined && !this.#closed) {
+				state.inlineInput.setText(value);
+				state.customInput = value;
 				state.selectedOptions.clear();
-				clearNoteUnlessRow(state, rowItem.key);
-				this.#advanceAfterQuestion();
+				clearNoteUnlessRow(state, "other");
 			}
 		} finally {
-			this.#promptActive = false;
-			this.#runDeferredTimeout();
+			this.#inputEditorOpen = false;
 			this.#requestRender();
+			this.#runDeferredTimeout();
 		}
 	}
 
@@ -948,7 +1021,7 @@ export class AskDialogComponent implements Component {
 
 	#handleTimeout(): void {
 		if (this.#closed) return;
-		if (this.#promptActive) {
+		if (this.#promptActive || this.#inputEditorOpen) {
 			this.#timeoutExpired = true;
 			return;
 		}
diff --git a/packages/coding-agent/src/modes/components/ask-inline-input.ts b/packages/coding-agent/src/modes/components/ask-inline-input.ts
new file mode 100644
index 0000000000..a506f8c7d7
--- /dev/null
+++ b/packages/coding-agent/src/modes/components/ask-inline-input.ts
@@ -0,0 +1,35 @@
+import { CURSOR_MARKER, wrapTextWithAnsi } from "@oh-my-pi/pi-tui";
+
+const graphemeSegmenter = new Intl.Segmenter(undefined, { granularity: "grapheme" });
+
+export interface RenderAskInlineInputOptions {
+	buffer: string;
+	cursorOffset: number | undefined;
+	rowPrefix: string;
+	continuationPrefix: string;
+	contentWidth: number;
+	selectedText: (text: string) => string;
+}
+
+function resolveCursorOffset(buffer: string, requested: number | undefined): number {
+	if (requested !== undefined && requested >= 0 && requested <= buffer.length) return requested;
+	return buffer.length;
+}
+
+function buildCursorRaw(buffer: string, offset: number): string {
+	const before = buffer.slice(0, offset);
+	const [firstGrapheme] = graphemeSegmenter.segment(buffer.slice(offset));
+	const rawAt = firstGrapheme ? firstGrapheme.segment : "";
+	const cursorAtLineEnd = rawAt === "\n";
+	const atCursor = rawAt === "" || rawAt === " " || cursorAtLineEnd ? "\xa0" : rawAt;
+	const after = buffer.slice(offset + (cursorAtLineEnd ? 0 : rawAt.length));
+	return `${before}${CURSOR_MARKER}\x1b[7m${atCursor}\x1b[27m${after}`;
+}
+
+export function renderAskInlineInputRow(options: RenderAskInlineInputOptions): string[] {
+	const { buffer, cursorOffset, rowPrefix, continuationPrefix, contentWidth, selectedText } = options;
+	const raw = buildCursorRaw(buffer, resolveCursorOffset(buffer, cursorOffset));
+	return wrapTextWithAnsi(raw, contentWidth).map((segment, index) =>
+		selectedText(`${index === 0 ? rowPrefix : continuationPrefix}${segment}`),
+	);
+}
diff --git a/packages/coding-agent/src/modes/controllers/extension-ui-controller.ts b/packages/coding-agent/src/modes/controllers/extension-ui-controller.ts
index 899fdad4c9..dca4bed3f0 100644
--- a/packages/coding-agent/src/modes/controllers/extension-ui-controller.ts
+++ b/packages/coding-agent/src/modes/controllers/extension-ui-controller.ts
@@ -29,12 +29,13 @@ import { HookSelectorComponent, type HookSelectorSlider } from "../../modes/comp
 import { getAvailableThemesWithPaths, getThemeByName, setTheme, type Theme, theme } from "../../modes/theme/theme";
 import type { InteractiveModeContext, InteractiveSelectorDialogOptions } from "../../modes/types";
 import { normalizeCustomMessagePayload, USER_INTERRUPT_LABEL } from "../../session/messages";
+import { getEditorCommand, openInEditor } from "../../utils/external-editor";
 import { setExtensionTerminalTitle, setSessionTerminalTitle } from "../../utils/title-generator";
 
 const MAX_WIDGET_LINES = 10;
-const ASK_OTHER_OPTION = "Other (type your own)";
+const ASK_OTHER_OPTION = "Type something.";
 const ASK_CHAT_OPTION = "Chat about this";
-const ASK_NEXT_OPTION = "Next →";
+const ASK_NEXT_OPTION = "Next";
 
 interface CollabDialogWinner {
 	source: "local" | "remote";
@@ -680,12 +681,36 @@ export class ExtensionUiController {
 				return promise;
 			};
 
+			const editInput = async (value: string): Promise<string | undefined> => {
+				const editorCommand = getEditorCommand();
+				if (!editorCommand) {
+					this.showHookNotify("no editor configured. set $visual or $editor.", "warning");
+					return undefined;
+				}
+				try {
+					this.ctx.ui.stop();
+					return (await openInEditor(editorCommand, value, { signal: dialogOptions?.signal })) ?? undefined;
+				} catch (error) {
+					if (!dialogOptions?.signal?.aborted) {
+						this.showHookNotify(
+							`external editor failed: ${error instanceof Error ? error.message : String(error)}`,
+							"error",
+						);
+					}
+					return undefined;
+				} finally {
+					this.ctx.ui.start();
+					this.ctx.ui.requestRender(true);
+				}
+			};
+
 			askDialog = new AskDialogComponent(
 				questions,
 				{
 					onSubmit: result => settle(result),
 					onCancel: () => settle(undefined),
 					onPrompt: promptForText,
+					onEditInput: editInput,
 				},
 				{
 					timeout: dialogOptions?.timeout,
@@ -775,15 +800,7 @@ export class ExtensionUiController {
 				const checkedIndices = question.options
 					.map((option, index) => (selected.has(option.label) ? index : -1))
 					.filter(index => index >= 0);
-				// Mirror the local dialog's Next gating: omit the Next option until
-				// at least one option is checked or a custom answer exists, so a
-				// guest cannot submit an empty multi-select result
-				// (PRRT_kwDOQxs0bc6OFbDW). The remote select has no "disabled" row
-				// concept, so we omit rather than dim it.
-				const hasAnswer = selected.size > 0 || customInput !== undefined;
-				const options = [...baseOptions, ASK_OTHER_OPTION];
-				if (hasAnswer) options.push(ASK_NEXT_OPTION);
-				options.push(ASK_CHAT_OPTION);
+				const options = [...baseOptions, ASK_OTHER_OPTION, ASK_NEXT_OPTION, ASK_CHAT_OPTION];
 				const choice = await this.#requestGuestUiString(
 					{
 						kind: "select",
@@ -792,9 +809,7 @@ export class ExtensionUiController {
 						selectionMarker: "checkbox",
 						checkedIndices,
 						markableCount: question.options.length,
-						helpText: hasAnswer
-							? "up/down navigate  enter toggle  Next → continue  esc cancel"
-							: "up/down navigate  enter toggle  esc cancel",
+						helpText: "up/down navigate  enter toggle  Next continue  esc cancel",
 					},
 					signal,
 				);
@@ -808,9 +823,10 @@ export class ExtensionUiController {
 						signal,
 					);
 					if (input.kind === "unavailable") return "unavailable";
-					// Guest cancelled the Other editor: keep the ask open and
+					// guest cancelled the custom editor: keep the ask open and
 					// return to the option list instead of cancelling the whole ask.
 					if (input.kind === "cancelled") continue;
+					selected.clear();
 					customInput = input.value;
 					break;
 				}
@@ -845,7 +861,7 @@ export class ExtensionUiController {
 						signal,
 					);
 					if (input.kind === "unavailable") return "unavailable";
-					// Guest cancelled the Other editor: re-show the select list
+					// guest cancelled the custom editor: re-show the select list
 					// instead of cancelling the whole ask.
 					if (input.kind === "cancelled") continue;
 					customInput = input.value;
diff --git a/packages/coding-agent/src/prompts/tools/ask.md b/packages/coding-agent/src/prompts/tools/ask.md
index 0a30e24746..a75cb070d1 100644
--- a/packages/coding-agent/src/prompts/tools/ask.md
+++ b/packages/coding-agent/src/prompts/tools/ask.md
@@ -1,6 +1,7 @@
 Ask user for clarification/input during task execution.
 
 <conditions>
+- the user starts a new project, application, service, library, or website. ask one batched questionnaire before planning or editing.
 - Multiple approaches with significantly different tradeoffs user should weigh.
 </conditions>
 
@@ -18,5 +19,5 @@ Ask user for clarification/input during task execution.
 <critical>
 - Default to action. Resolve ambiguity via repo conventions, existing patterns, reasonable defaults. Exhaust existing sources (code, configs, docs, history) before asking. Ask only when options have materially different tradeoffs the user must decide.
 - If multiple choices acceptable: pick most conservative/standard option; proceed; state choice.
-- Do NOT include "Other"; UI automatically adds "Other (type your own)" to every question.
+- do not include "other" or "type something."; the ui adds an inline `type something.` row to every question.
 </critical>
diff --git a/packages/coding-agent/src/session/agent-session.ts b/packages/coding-agent/src/session/agent-session.ts
index 5b0475f3cb..d63a0ae6a3 100644
--- a/packages/coding-agent/src/session/agent-session.ts
+++ b/packages/coding-agent/src/session/agent-session.ts
@@ -421,6 +421,13 @@ type SetSessionNameWithTrigger = (
 ) => Promise<boolean>;
 
 const kPersistedSessionEntryId = Symbol("persistedSessionEntryId");
+
+const newProjectPrompt =
+	/\b(?:bootstrap|build|create|develop|initialize|make|scaffold|set\s+up|start)\s+(?:(?:me|us)\s+)?(?:(?:a|an|my|our)\s+)?(?:brand[- ]new\s+|new\s+)?(?:[\p{L}\p{N}.+#-]+\s+){0,3}(?:api|app(?:lication)?|cli|codebase|dashboard|extension|library|package|plugin|project|repo(?:sitory)?|service|site|tool|website)\b/iu;
+
+export function initiatesNewProject(text: string): boolean {
+	return newProjectPrompt.test(text);
+}
 type PersistedAssistantMessage = AssistantMessage & { [kPersistedSessionEntryId]?: string };
 
 export class AgentSession {
@@ -499,6 +506,9 @@ export class AgentSession {
 	#titleSystemPrompt: string | undefined;
 	#titleGenerationAbortController = new AbortController();
 	#toolChoiceQueue = new ToolChoiceQueue();
+	#newProjectAskRequirement: { id: string } | undefined;
+	#newProjectAskSequence = 0;
+	#queuedNewProjectPrompts = new WeakSet<AgentMessage>();
 
 	readonly #bash: BashRunner;
 
@@ -1063,17 +1073,24 @@ export class AgentSession {
 			withBashBranchTransition: operation => this.#bash.withBranchTransition(operation),
 		};
 		this.#recovery = new TurnRecovery(recoveryHost, { initialRetryFallback: config.initialRetryFallback });
-		this.#detachUsageBeforeQueueDequeue = this.agent.addBeforeQueuedMessageDequeueHook(async signal => {
+		this.#detachUsageBeforeQueueDequeue = this.agent.addBeforeQueuedMessageDequeueHook(async (signal, kind) => {
 			if (
-				!this.settings.get("retry.usageAwareFallback") ||
-				(this.#usagePreflightReadyForNextModelCall && this.#usagePreflightReadyModel === this.model)
+				this.settings.get("retry.usageAwareFallback") &&
+				(!this.#usagePreflightReadyForNextModelCall || this.#usagePreflightReadyModel !== this.model) &&
+				!(await this.#runQueuedUsageAwarePreflight(signal))
 			) {
-				return;
-			}
-			if (!(await this.#runQueuedUsageAwarePreflight(signal))) {
 				signal?.throwIfAborted();
 				throw new DOMException("Usage preflight cancelled", "AbortError");
 			}
+			signal?.throwIfAborted();
+			const queue = kind === "steer" ? this.agent.peekSteeringQueue() : this.agent.peekFollowUpQueue();
+			const dequeueHeadOnly = queue.some(message => this.#queuedNewProjectPrompts.has(message));
+			const firstMessage = queue[0];
+			const startsNewProject = firstMessage !== undefined && this.#queuedNewProjectPrompts.delete(firstMessage);
+			if (startsNewProject && this.getActiveToolNames().includes("ask")) {
+				this.#newProjectAskRequirement = { id: `new-project-ask:${++this.#newProjectAskSequence}` };
+			}
+			return dequeueHeadOnly;
 		});
 		this.#detachUsageBeforeModelCall = this.agent.addBeforeModelCallHook(async signal => {
 			if (!this.settings.get("retry.usageAwareFallback")) return;
@@ -1623,6 +1640,26 @@ export class AgentSession {
 				reminder: [buildResolveReminderMessage(head.sourceToolName)],
 			};
 		}
+		const newProjectAsk = this.#newProjectAskRequirement;
+		if (newProjectAsk !== undefined && this.getActiveToolNames().includes("ask")) {
+			const reminder: Message = {
+				role: "developer",
+				content: [
+					{
+						type: "text",
+						text: "this request starts a new project. call the ask tool now with one batched questionnaire before planning, editing, or using another tool.",
+					},
+				],
+				attribution: "agent",
+				timestamp: Date.now(),
+			};
+			return {
+				soft: true,
+				id: newProjectAsk.id,
+				toolName: "ask",
+				reminder: [reminder],
+			};
+		}
 		return undefined;
 	}
 
@@ -2077,6 +2114,15 @@ export class AgentSession {
 	 * event/persistence pipeline during teardown.
 	 */
 	#handleAgentEvent = (event: AgentEvent): Promise<void> => {
+		if (
+			event.type === "tool_execution_end" &&
+			event.toolName === "ask" &&
+			this.#newProjectAskRequirement !== undefined
+		) {
+			this.#newProjectAskRequirement = event.isError
+				? { id: `new-project-ask:${++this.#newProjectAskSequence}` }
+				: undefined;
+		}
 		const processing = this.#dispatchAgentEvent(event);
 		this.#inFlightEventHandlers.add(processing);
 		void processing.finally(() => this.#inFlightEventHandlers.delete(processing)).catch(() => {});
@@ -5181,8 +5227,20 @@ export class AgentSession {
 			return true;
 		}
 
-		// Skip eager preludes when the user has already queued a directive
-		const hasPendingUserDirective = this.#toolChoiceQueue.inspect().includes("user-force");
+		const newProjectAskRequirementId =
+			!options?.synthetic &&
+			options?.toolChoice === undefined &&
+			initiatesNewProject(expandedText) &&
+			this.getActiveToolNames().includes("ask")
+				? `new-project-ask:${++this.#newProjectAskSequence}`
+				: undefined;
+		if (newProjectAskRequirementId !== undefined) {
+			this.#newProjectAskRequirement = { id: newProjectAskRequirementId };
+		}
+
+		// skip eager preludes while the new-project questionnaire owns the first turn.
+		const hasPendingUserDirective =
+			this.#toolChoiceQueue.inspect().includes("user-force") || newProjectAskRequirementId !== undefined;
 		const activeModel = this.agent.state.model;
 		const externalThinkingToolChoice =
 			!options?.synthetic &&
@@ -5770,23 +5828,22 @@ export class AgentSession {
 			? await this.#buildImageDescriptionNotice(normalizedImages)
 			: undefined;
 		this.#allowQueuedMessageDrainRetry();
+		const message: AgentMessage = {
+			role: "user",
+			content,
+			...(mode === "steer" ? { steering: true } : {}),
+			attribution: "user",
+			timestamp: Date.now(),
+		};
+		if (initiatesNewProject(text) && this.getActiveToolNames().includes("ask")) {
+			this.#queuedNewProjectPrompts.add(message);
+		}
 		if (mode === "followUp") {
 			if (imageDescriptionNotice) this.agent.followUp(imageDescriptionNotice);
-			this.agent.followUp({
-				role: "user",
-				content,
-				attribution: "user",
-				timestamp: Date.now(),
-			});
+			this.agent.followUp(message);
 		} else {
 			if (imageDescriptionNotice) this.agent.steer(imageDescriptionNotice);
-			this.agent.steer({
-				role: "user",
-				content,
-				steering: true,
-				attribution: "user",
-				timestamp: Date.now(),
-			});
+			this.agent.steer(message);
 		}
 		this.#scheduleIdleQueueDrain();
 	}
diff --git a/packages/coding-agent/src/tools/ask.ts b/packages/coding-agent/src/tools/ask.ts
index 0730a207cc..ecb57095fb 100644
--- a/packages/coding-agent/src/tools/ask.ts
+++ b/packages/coding-agent/src/tools/ask.ts
@@ -8,11 +8,11 @@
  *   3. Get decisions on implementation choices as you work
  *   4. Offer choices to the user about what direction to take
  *
- * Usage notes:
- *   - Users will always be able to select "Other" to provide custom text input
- *   - Use multi: true to allow multiple answers to be selected for a question
- *   - Use recommended: <index> to mark the default option; "(Recommended)" suffix is added automatically
- *   - Questions may time out and auto-select the recommended option (configurable, disabled in plan mode)
+ * usage notes:
+ *   - every question includes an inline "type something." row for custom text
+ *   - use multi: true to allow multiple answers to be selected for a question
+ *   - use recommended: <index> to mark the default option; "(recommended)" is added automatically
+ *   - questions may time out and auto-select the recommended option (configurable, disabled in plan mode)
  */
 
 import { type as arkType } from "@oh-my-pi/omptype";
@@ -45,13 +45,16 @@ import { ToolAbortError } from "./tool-errors";
 // Types
 // =============================================================================
 
-const OTHER_OPTION = "Other (type your own)";
+const OTHER_OPTION = "Type something.";
 const CHAT_ABOUT_THIS_OPTION = "Chat about this";
-const NEXT_OPTION = "Next →";
+const NEXT_OPTION = "Next";
 const RESERVED_OPTION_LABELS: Record<string, true> = {
+	Other: true,
+	"Other (type your own)": true,
 	[OTHER_OPTION]: true,
 	[CHAT_ABOUT_THIS_OPTION]: true,
 	[NEXT_OPTION]: true,
+	"Next →": true,
 };
 
 const OptionItem = arkType({
diff --git a/packages/coding-agent/src/utils/external-editor.ts b/packages/coding-agent/src/utils/external-editor.ts
index 287f10c8a5..d04d7f5d41 100644
--- a/packages/coding-agent/src/utils/external-editor.ts
+++ b/packages/coding-agent/src/utils/external-editor.ts
@@ -32,6 +32,8 @@ export interface OpenInEditorOptions {
 	stdio?: [number | "inherit", number | "inherit", number | "inherit"];
 	/** Keep the file's trailing newline instead of trimming it from the returned text. */
 	trimTrailingNewline?: boolean;
+	/** abort the editor child when its owning dialog closes. */
+	signal?: AbortSignal;
 }
 
 /**
@@ -49,18 +51,55 @@ export async function openInEditor(
 	const tmpFile = path.join(os.tmpdir(), `omp-editor-${Snowflake.next()}${ext}`);
 
 	try {
+		options?.signal?.throwIfAborted();
 		await Bun.write(tmpFile, content);
+		options?.signal?.throwIfAborted();
 
 		const [editor, ...editorArgs] = editorCmd.split(" ");
 		const stdio = options?.stdio ?? ["inherit", "inherit", "inherit"];
+		const detached = process.platform !== "win32";
 		const child =
 			process.platform === "win32"
 				? spawn(editor, [...editorArgs, tmpFile], { stdio, shell: true })
-				: spawn("/bin/sh", ["-c", `${editorCmd} "$1"`, "sh", tmpFile], { stdio });
+				: spawn("/bin/sh", ["-c", `exec ${editorCmd} "$1"`, "sh", tmpFile], { stdio, detached });
 		const { promise, reject, resolve } = Promise.withResolvers<number>();
-		child.once("exit", (code, signal) => resolve(code ?? (signal ? -1 : 0)));
-		child.once("error", error => reject(error));
-		const exitCode = await promise;
+		let aborted = false;
+		let abortedError: Error | undefined;
+		const abort = (): void => {
+			aborted = true;
+			abortedError =
+				options?.signal?.reason instanceof Error
+					? options.signal.reason
+					: new DOMException("the operation was aborted", "AbortError");
+			if (child.pid === undefined) return;
+			if (detached) {
+				try {
+					process.kill(-child.pid, "SIGKILL");
+				} catch {
+					child.kill("SIGKILL");
+				}
+				return;
+			}
+			const killer = spawn("taskkill.exe", ["/pid", String(child.pid), "/t", "/f"], {
+				stdio: "ignore",
+				windowsHide: true,
+			});
+			killer.once("error", () => child.kill("SIGKILL"));
+			killer.unref();
+		};
+		child.once("exit", (code, signal) => {
+			if (aborted) reject(abortedError);
+			else resolve(code ?? (signal ? -1 : 0));
+		});
+		child.once("error", error => reject(aborted ? abortedError : error));
+		if (options?.signal?.aborted) abort();
+		else options?.signal?.addEventListener("abort", abort, { once: true });
+		let exitCode: number;
+		try {
+			exitCode = await promise;
+		} finally {
+			options?.signal?.removeEventListener("abort", abort);
+		}
 
 		if (exitCode === 0) {
 			const text = await Bun.file(tmpFile).text();
diff --git a/packages/coding-agent/test/agent-session-force-tool-choice.test.ts b/packages/coding-agent/test/agent-session-force-tool-choice.test.ts
index d9dd6814d2..266d458298 100644
--- a/packages/coding-agent/test/agent-session-force-tool-choice.test.ts
+++ b/packages/coding-agent/test/agent-session-force-tool-choice.test.ts
@@ -17,6 +17,10 @@ let authStorage: AuthStorage | undefined;
 let session: AgentSession;
 let sessionManager: SessionManager;
 let mock: MockModel;
+let askRuns: ReturnType<typeof vi.fn>;
+let bashRuns: ReturnType<typeof vi.fn>;
+let askFailures: number;
+let abortAfterAskFailure: boolean;
 
 beforeEach(async () => {
 	tempDir = TempDir.createSync("@pi-agent-session-force-tool-");
@@ -31,12 +35,19 @@ beforeEach(async () => {
 
 	const emptyObjectSchema = type("object");
 
+	askRuns = vi.fn();
+	bashRuns = vi.fn();
+	askFailures = 0;
+	abortAfterAskFailure = false;
 	const bashTool: AgentTool = {
 		name: "bash",
 		label: "Bash",
 		description: "Mock bash tool",
 		parameters: emptyObjectSchema,
-		execute: async () => ({ content: [{ type: "text" as const, text: "ok" }] }),
+		execute: async () => {
+			bashRuns();
+			return { content: [{ type: "text" as const, text: "ok" }] };
+		},
 	};
 	const writeTool: AgentTool = {
 		name: "write",
@@ -45,6 +56,21 @@ beforeEach(async () => {
 		parameters: emptyObjectSchema,
 		execute: async () => ({ content: [{ type: "text" as const, text: "ok" }] }),
 	};
+	const askTool: AgentTool = {
+		name: "ask",
+		label: "ask",
+		description: "mock ask tool",
+		parameters: emptyObjectSchema,
+		execute: async () => {
+			askRuns();
+			if (askFailures > 0) {
+				askFailures--;
+				if (abortAfterAskFailure) session.agent.abort("test ask failure");
+				throw new Error("ask failed");
+			}
+			return { content: [{ type: "text" as const, text: "answered" }] };
+		},
+	};
 
 	mock = createMockModel({ handler: () => ({ content: ["done"] }) });
 
@@ -54,7 +80,7 @@ beforeEach(async () => {
 		initialState: {
 			model,
 			systemPrompt: ["Test"],
-			tools: [bashTool, writeTool],
+			tools: [askTool, bashTool, writeTool],
 			messages: [],
 		},
 		convertToLlm,
@@ -68,6 +94,7 @@ beforeEach(async () => {
 		modelRegistry,
 		toolRegistry: new Map([
 			[bashTool.name, bashTool],
+			[askTool.name, askTool],
 			[writeTool.name, writeTool],
 		]),
 	});
@@ -102,6 +129,158 @@ it("forces specific tool, then transitions to none, then clears", () => {
 	expect(third).toBeUndefined();
 });
 
+it("requires ask before a new-project prompt can use another tool", async () => {
+	mock.push(() => ({ content: [{ type: "toolCall", id: "detour", name: "bash", arguments: {} }] }));
+	mock.push(() => ({ content: [{ type: "toolCall", id: "questionnaire", name: "ask", arguments: {} }] }));
+	mock.push(() => ({ content: ["done"] }));
+
+	await session.prompt("build me a new website");
+
+	expect(askRuns).toHaveBeenCalledTimes(1);
+	expect(bashRuns).not.toHaveBeenCalled();
+	expect(mock.calls).toHaveLength(3);
+	expect(mock.calls[0]?.options?.toolChoice).toBeUndefined();
+	expect(mock.calls[1]?.options?.toolChoice).toEqual({ type: "tool", name: "ask" });
+	expect(mock.calls[2]?.options?.toolChoice).toBeUndefined();
+});
+
+it("retains the questionnaire requirement after a failed ask", async () => {
+	askFailures = 1;
+	mock.push(() => ({ content: [{ type: "toolCall", id: "failed-ask", name: "ask", arguments: {} }] }));
+	mock.push(() => ({ content: [{ type: "toolCall", id: "detour", name: "bash", arguments: {} }] }));
+	mock.push(() => ({ content: [{ type: "toolCall", id: "successful-ask", name: "ask", arguments: {} }] }));
+	mock.push(() => ({ content: ["done"] }));
+
+	await session.prompt("create a react app");
+
+	expect(askRuns).toHaveBeenCalledTimes(2);
+	expect(bashRuns).not.toHaveBeenCalled();
+	expect(mock.calls[2]?.options?.toolChoice).toEqual({ type: "tool", name: "ask" });
+});
+
+it("retains the questionnaire requirement after an aborted failed-ask run", async () => {
+	askFailures = 1;
+	abortAfterAskFailure = true;
+	mock.push(() => ({ content: [{ type: "toolCall", id: "failed-ask", name: "ask", arguments: {} }] }));
+
+	await expect(session.prompt("create a react app")).resolves.toBe(true);
+
+	abortAfterAskFailure = false;
+	mock.push(() => ({ content: [{ type: "toolCall", id: "detour", name: "bash", arguments: {} }] }));
+	mock.push(() => ({ content: [{ type: "toolCall", id: "successful-ask", name: "ask", arguments: {} }] }));
+	mock.push(() => ({ content: ["done"] }));
+	await session.prompt("continue the request");
+
+	expect(askRuns).toHaveBeenCalledTimes(2);
+	expect(bashRuns).not.toHaveBeenCalled();
+	expect(mock.calls.at(-2)?.options?.toolChoice).toEqual({ type: "tool", name: "ask" });
+});
+
+it("does not turn an unrelated failed ask into a project requirement", async () => {
+	askFailures = 1;
+	abortAfterAskFailure = true;
+	mock.push(() => ({ content: [{ type: "toolCall", id: "failed-ask", name: "ask", arguments: {} }] }));
+
+	await expect(session.prompt("inspect the current code")).resolves.toBe(true);
+
+	abortAfterAskFailure = false;
+	mock.push(() => ({ content: [{ type: "toolCall", id: "bash", name: "bash", arguments: {} }] }));
+	await session.prompt("continue the request");
+
+	expect(askRuns).toHaveBeenCalledTimes(1);
+	expect(bashRuns).toHaveBeenCalledTimes(1);
+});
+
+it("keeps the ask requirement with a queued new-project follow-up", async () => {
+	const currentTurn = Promise.withResolvers<{ content: string[] }>();
+	const currentTurnStarted = Promise.withResolvers<void>();
+	mock.push(async () => {
+		currentTurnStarted.resolve();
+		return currentTurn.promise;
+	});
+	mock.push(() => ({ content: [{ type: "toolCall", id: "detour", name: "bash", arguments: {} }] }));
+	mock.push(() => ({ content: [{ type: "toolCall", id: "questionnaire", name: "ask", arguments: {} }] }));
+	mock.push(() => ({ content: ["done"] }));
+
+	const running = session.prompt("inspect the current code");
+	await currentTurnStarted.promise;
+	await session.prompt("create a new cli", { streamingBehavior: "followUp" });
+	currentTurn.resolve({ content: ["current work done"] });
+	await running;
+
+	expect(askRuns).toHaveBeenCalledTimes(1);
+	expect(bashRuns).not.toHaveBeenCalled();
+	expect(mock.calls[2]?.options?.toolChoice).toEqual({ type: "tool", name: "ask" });
+});
+
+it("requires a separate ask for each all-mode new-project follow-up", async () => {
+	const currentTurn = Promise.withResolvers<{ content: string[] }>();
+	const currentTurnStarted = Promise.withResolvers<void>();
+	mock.push(async () => {
+		currentTurnStarted.resolve();
+		return currentTurn.promise;
+	});
+	mock.push(() => ({ content: [{ type: "toolCall", id: "detour-1", name: "bash", arguments: {} }] }));
+	mock.push(() => ({ content: [{ type: "toolCall", id: "questionnaire-1", name: "ask", arguments: {} }] }));
+	mock.push(() => ({ content: ["first project done"] }));
+	mock.push(() => ({ content: [{ type: "toolCall", id: "detour-2", name: "bash", arguments: {} }] }));
+	mock.push(() => ({ content: [{ type: "toolCall", id: "questionnaire-2", name: "ask", arguments: {} }] }));
+	mock.push(() => ({ content: ["second project done"] }));
+	session.agent.setFollowUpMode("all");
+
+	const running = session.prompt("inspect the current code");
+	await currentTurnStarted.promise;
+	await session.prompt("create a new cli", { streamingBehavior: "followUp" });
+	await session.prompt("build me a new website", { streamingBehavior: "followUp" });
+	currentTurn.resolve({ content: ["current work done"] });
+	await running;
+
+	expect(askRuns).toHaveBeenCalledTimes(2);
+	expect(bashRuns).not.toHaveBeenCalled();
+	expect(mock.calls[2]?.options?.toolChoice).toEqual({ type: "tool", name: "ask" });
+	expect(mock.calls[5]?.options?.toolChoice).toEqual({ type: "tool", name: "ask" });
+	const projectCallIndexes = ["create a new cli", "build me a new website"].map(text =>
+		mock.calls.findIndex(call =>
+			call.context.messages.some(message => JSON.stringify(message.content).includes(text)),
+		),
+	);
+	expect(projectCallIndexes.every(index => index >= 0)).toBe(true);
+	expect(projectCallIndexes[0]).not.toBe(projectCallIndexes[1]);
+});
+
+it("keeps mixed steering and follow-up project markers with their messages", async () => {
+	const currentTurn = Promise.withResolvers<{ content: string[] }>();
+	const currentTurnStarted = Promise.withResolvers<void>();
+	mock.push(async () => {
+		currentTurnStarted.resolve();
+		return currentTurn.promise;
+	});
+	mock.push(() => ({ content: [{ type: "toolCall", id: "detour-1", name: "bash", arguments: {} }] }));
+	mock.push(() => ({ content: [{ type: "toolCall", id: "questionnaire-1", name: "ask", arguments: {} }] }));
+	mock.push(() => ({ content: ["first project done"] }));
+	mock.push(() => ({ content: [{ type: "toolCall", id: "detour-2", name: "bash", arguments: {} }] }));
+	mock.push(() => ({ content: [{ type: "toolCall", id: "questionnaire-2", name: "ask", arguments: {} }] }));
+	mock.push(() => ({ content: ["second project done"] }));
+
+	const running = session.prompt("inspect the current code");
+	await currentTurnStarted.promise;
+	await session.prompt("remember this constraint", { streamingBehavior: "steer" });
+	await session.prompt("create a new cli", { streamingBehavior: "steer" });
+	await session.prompt("build me a new website", { streamingBehavior: "followUp" });
+	currentTurn.resolve({ content: ["current work done"] });
+	await running;
+
+	expect(askRuns).toHaveBeenCalledTimes(2);
+	expect(bashRuns).not.toHaveBeenCalled();
+	const projectCallIndexes = ["create a new cli", "build me a new website"].map(text =>
+		mock.calls.findIndex(call =>
+			call.context.messages.some(message => JSON.stringify(message.content).includes(text)),
+		),
+	);
+	expect(projectCallIndexes.every(index => index >= 0)).toBe(true);
+	expect(projectCallIndexes[0]).not.toBe(projectCallIndexes[1]);
+});
+
 it("drops an unavailable forced choice with the rest of its sequence", async () => {
 	session.setForcedToolChoice("write");
 
diff --git a/packages/coding-agent/test/collab/guest-ui-request.test.ts b/packages/coding-agent/test/collab/guest-ui-request.test.ts
index 8c465c88b3..d962b18a82 100644
--- a/packages/coding-agent/test/collab/guest-ui-request.test.ts
+++ b/packages/coding-agent/test/collab/guest-ui-request.test.ts
@@ -758,14 +758,11 @@ describe("guest ask unavailable literal answer (#4375)", () => {
 	});
 });
 
-// ── Guest ask multi-select Next gating (#4375: PRRT_kwDOQxs0bc6OFbDW) ───────
+// guest ask multi-select sentinels
 //
-// The local rich dialog disables the Next row on a single-question
-// multi-select until at least one option or custom input is chosen. The guest
-// mirror has no "disabled row" concept on the wire, so it must OMIT Next from
-// the option list until an answer exists, then include it on the next round.
-// This test pins that wire-level contract by inspecting consecutive
-// ui-request frames.
+// the remote mirror uses the same type-something and next rows as the local
+// dialog. next stays available so a guest can submit an unanswered question,
+// matching the local submit tab's partial-answer behavior.
 
 /** Context double with the extra members `#showLocalAskDialog` touches when
  *  mounting the local AskDialogComponent. The local dialog is never driven
@@ -792,7 +789,7 @@ function makeAskHostContext(): InteractiveModeContext {
 	return stub as unknown as InteractiveModeContext;
 }
 
-describe("guest ask multi-select Next gating (#4375 PRRT_kwDOQxs0bc6OFbDW)", () => {
+describe("guest ask multi-select sentinels", () => {
 	/** Skip ui-request-end dismissal frames, wait for the next ui-request. */
 	async function nextUiRequest(guest: {
 		nextFrame(): Promise<CollabFrame>;
@@ -812,7 +809,7 @@ describe("guest ask multi-select Next gating (#4375 PRRT_kwDOQxs0bc6OFbDW)", ()
 		return frame.request.options.map(o => (typeof o === "string" ? o : o.label));
 	}
 
-	it("omits Next from the first ui-request, includes it after a toggle", async () => {
+	it("offers type-something and next rows before and after a toggle", async () => {
 		const ctx = makeAskHostContext();
 		const host = new CollabHost(ctx);
 		await host.start("ws://localhost:8787");
@@ -833,25 +830,22 @@ describe("guest ask multi-select Next gating (#4375 PRRT_kwDOQxs0bc6OFbDW)", ()
 			];
 			const result = controller.showAskDialog(questions);
 
-			// First ui-request: Next must be absent (no answer yet).
 			const first = await nextUiRequest(guest);
 			const firstLabels = selectLabels(first);
-			expect(firstLabels).not.toContain("Next →");
+			expect(firstLabels).toContain("Next");
 			expect(firstLabels).toContain("Option A");
-			expect(firstLabels).toContain("Other (type your own)");
+			expect(firstLabels).toContain("Type something.");
 			expect(firstLabels).toContain("Chat about this");
 
-			// Guest toggles Option A — a real answer, not Next/Other/Chat.
+			// the guest toggles one option.
 			guest.socket.send({ t: "ui-response", reqId: first.request.reqId, value: "Option A" });
 
-			// Second ui-request: Next must now be present.
 			const second = await nextUiRequest(guest);
 			const secondLabels = selectLabels(second);
-			expect(secondLabels).toContain("Next →");
+			expect(secondLabels).toContain("Next");
 			expect(secondLabels).toContain("Option A");
 
-			// Guest selects Next to submit.
-			guest.socket.send({ t: "ui-response", reqId: second.request.reqId, value: "Next →" });
+			guest.socket.send({ t: "ui-response", reqId: second.request.reqId, value: "Next" });
 			const settled = await result;
 			expect(settled?.kind).toBe("submit");
 			if (settled?.kind === "submit") {
diff --git a/packages/coding-agent/test/external-editor.test.ts b/packages/coding-agent/test/external-editor.test.ts
index 4e1b7ab17e..31c347095f 100644
--- a/packages/coding-agent/test/external-editor.test.ts
+++ b/packages/coding-agent/test/external-editor.test.ts
@@ -81,4 +81,65 @@ describe("openInEditor", () => {
 			await tempDir.remove();
 		}
 	});
+
+	it.skipIf(process.platform === "win32")("does not launch an editor for a pre-aborted dialog", async () => {
+		const tempDir = TempDir.createSync("@external-editor-pre-abort-");
+		try {
+			const marker = path.join(tempDir.path(), "launched");
+			const editorPath = path.join(tempDir.path(), "editor");
+			await Bun.write(editorPath, `#!/bin/sh\ntouch "${marker}"\n`);
+			fs.chmodSync(editorPath, 0o755);
+			const controller = new AbortController();
+			controller.abort();
+
+			await expect(openInEditor(`"${editorPath}"`, "original", { signal: controller.signal })).rejects.toMatchObject(
+				{
+					name: "AbortError",
+				},
+			);
+			expect(fs.existsSync(marker)).toBe(false);
+		} finally {
+			await tempDir.remove();
+		}
+	});
+
+	it.skipIf(process.platform === "win32")("aborts the complete editor process tree", async () => {
+		const tempDir = TempDir.createSync("@external-editor-abort-");
+		const editorPath = path.join(tempDir.path(), "editor");
+		const pidFile = path.join(tempDir.path(), "child.pid");
+		let childPid: number | undefined;
+		const childStarted = Promise.withResolvers<void>();
+		const watcher = fs.watch(tempDir.path(), (_event, filename) => {
+			if (String(filename) === "child.pid") childStarted.resolve();
+		});
+		try {
+			await Bun.write(editorPath, `#!/bin/sh\nsleep 30 &\necho $! > "${pidFile}"\nwait\n`);
+			fs.chmodSync(editorPath, 0o755);
+			const controller = new AbortController();
+			const editing = openInEditor(`"${editorPath}"`, "original", { signal: controller.signal });
+			if (!fs.existsSync(pidFile)) await childStarted.promise;
+			childPid = Number.parseInt(await Bun.file(pidFile).text(), 10);
+
+			controller.abort();
+			await expect(editing).rejects.toMatchObject({ name: "AbortError" });
+
+			let childAlive = true;
+			try {
+				process.kill(childPid, 0);
+			} catch {
+				childAlive = false;
+			}
+			expect(childAlive).toBe(false);
+		} finally {
+			watcher.close();
+			if (childPid !== undefined) {
+				try {
+					process.kill(childPid, "SIGKILL");
+				} catch {
+					// already terminated
+				}
+			}
+			await tempDir.remove();
+		}
+	});
 });
diff --git a/packages/coding-agent/test/modes/components/ask-dialog.test.ts b/packages/coding-agent/test/modes/components/ask-dialog.test.ts
index aa5251d597..bf0205c51b 100644
--- a/packages/coding-agent/test/modes/components/ask-dialog.test.ts
+++ b/packages/coding-agent/test/modes/components/ask-dialog.test.ts
@@ -15,6 +15,8 @@ const CANCEL = "\x07";
 const SPACE = " ";
 const TAB = "\t";
 const SHIFT_TAB = "\x1b[Z";
+const ctrlRightBracket = "\x1d";
+const ctrlG = "\x07";
 
 let darkTheme = await getThemeByName("dark");
 
@@ -78,6 +80,106 @@ describe("AskDialogComponent", () => {
 		});
 	});
 
+	it("edits the type-something row inline and submits its text", () => {
+		const onSubmit = vi.fn();
+		const component = new AskDialogComponent(
+			[
+				{
+					id: "q1",
+					question: "name?",
+					options: [{ label: "default" }, { label: "second" }],
+				},
+			],
+			{ onSubmit, onCancel: vi.fn(), onPrompt: vi.fn() },
+		);
+
+		component.handleInput(DOWN);
+		component.handleInput(DOWN);
+		component.handleInput("h");
+		component.handleInput("i");
+		expect(render(component)).toContain("hi");
+		component.handleInput(ENTER);
+
+		expect(onSubmit.mock.calls[0][0].results[0]).toMatchObject({
+			selectedOptions: [],
+			customInput: "hi",
+		});
+	});
+
+	it("collapses to one line and expands without losing state", () => {
+		const component = new AskDialogComponent(
+			[
+				{
+					id: "q1",
+					question: "name?",
+					options: [{ label: "default" }],
+				},
+			],
+			{ onSubmit: vi.fn(), onCancel: vi.fn(), onPrompt: vi.fn() },
+		);
+
+		const expandedRows = component.render(80).length;
+		component.handleInput(ctrlRightBracket);
+		expect(component.render(80)).toHaveLength(1);
+		expect(render(component)).toContain("ctrl+] to expand");
+		component.handleInput(ctrlRightBracket);
+		expect(component.render(80)).toHaveLength(expandedRows);
+	});
+
+	it("opens the external editor from inline custom input", async () => {
+		const onSubmit = vi.fn();
+		const onEditInput = vi.fn(async () => "edited");
+		const component = new AskDialogComponent(
+			[
+				{
+					id: "q1",
+					question: "name?",
+					options: [{ label: "default" }],
+				},
+			],
+			{ onSubmit, onCancel: vi.fn(), onPrompt: vi.fn(), onEditInput } as never,
+		);
+
+		component.handleInput(DOWN);
+		component.handleInput("draft");
+		component.handleInput(ctrlG);
+		await Promise.resolve();
+		await Promise.resolve();
+		expect(onEditInput).toHaveBeenCalledWith("draft");
+		component.handleInput(ENTER);
+		expect(onSubmit.mock.calls[0][0].results[0].customInput).toBe("edited");
+	});
+
+	it("external custom input replaces checked multi-select options", async () => {
+		const onSubmit = vi.fn();
+		const component = new AskDialogComponent(
+			[
+				{
+					id: "q1",
+					question: "choose?",
+					options: [{ label: "default" }, { label: "second" }],
+					multi: true,
+				},
+			],
+			{ onSubmit, onCancel: vi.fn(), onPrompt: vi.fn(), onEditInput: vi.fn(async () => "edited") },
+		);
+
+		component.handleInput(SPACE);
+		component.handleInput(DOWN);
+		component.handleInput(DOWN);
+		component.handleInput("draft");
+		component.handleInput(ctrlG);
+		await Promise.resolve();
+		await Promise.resolve();
+		component.handleInput(DOWN);
+		component.handleInput(ENTER);
+
+		expect(onSubmit.mock.calls[0][0].results[0]).toMatchObject({
+			selectedOptions: [],
+			customInput: "edited",
+		});
+	});
+
 	it("single-question, single-select: Space does not submit the highlighted answer", () => {
 		const onSubmit = vi.fn();
 		const questions: ExtensionAskDialogQuestion[] = [
@@ -551,91 +653,108 @@ describe("AskDialogComponent", () => {
 		expect(onSubmit.mock.calls[0][0].results[0].note).toBeUndefined();
 	});
 
-	it("shows selected multi-select options together with custom input on Submit", async () => {
-		const onPrompt = vi.fn().mockReturnValue(Promise.resolve("custom detail"));
+	it("custom input replaces checked options for a multi-select question", () => {
 		const onSubmit = vi.fn();
 		const questions: ExtensionAskDialogQuestion[] = [
 			{
 				id: "q1",
-				question: "Choose multiple?",
-				options: [{ label: "Option A" }, { label: "Option B" }],
+				question: "choose multiple?",
+				options: [{ label: "option a" }, { label: "option b" }],
 				multi: true,
 			},
 			{
 				id: "q2",
-				question: "Second question?",
-				options: [{ label: "Option C" }],
+				question: "second question?",
+				options: [{ label: "option c" }],
 			},
 		];
-
 		const component = new AskDialogComponent(questions, {
 			onSubmit,
 			onCancel: vi.fn(),
-			onPrompt,
+			onPrompt: vi.fn(),
 		});
 
 		component.handleInput(SPACE);
 		component.handleInput(DOWN);
 		component.handleInput(DOWN);
+		component.handleInput("custom detail");
+		component.handleInput(ENTER);
+		component.handleInput(TAB);
+		expect(render(component)).toContain("custom detail");
 		component.handleInput(ENTER);
-		await Promise.resolve();
-		await Promise.resolve();
 
-		// Multi questions do not auto-advance after the Other prompt: still on
-		// q1, so Tab twice (q2, then Submit) to reach the review.
+		expect(onSubmit.mock.calls[0][0].results[0]).toMatchObject({
+			selectedOptions: [],
+			customInput: "custom detail",
+		});
+	});
+
+	it("a multi-select option replaces an earlier custom answer", () => {
+		const onSubmit = vi.fn();
+		const component = new AskDialogComponent(
+			[
+				{
+					id: "q1",
+					question: "choose multiple?",
+					options: [{ label: "option a" }, { label: "option b" }],
+					multi: true,
+				},
+				{ id: "q2", question: "second question?", options: [{ label: "option c" }] },
+			],
+			{ onSubmit, onCancel: vi.fn(), onPrompt: vi.fn() },
+		);
+
+		component.handleInput(DOWN);
+		component.handleInput(DOWN);
+		component.handleInput("custom detail");
+		component.handleInput(ENTER);
+		component.handleInput(SHIFT_TAB);
+		component.handleInput(UP);
+		component.handleInput(SPACE);
 		component.handleInput(TAB);
 		component.handleInput(TAB);
-		const review = render(component);
-		expect(review).toContain("Option A");
-		expect(review).toContain("custom detail");
-
 		component.handleInput(ENTER);
 
-		expect(onSubmit).toHaveBeenCalledTimes(1);
-		expect(onSubmit.mock.calls[0][0].results[0].selectedOptions).toEqual(["Option A"]);
-		expect(onSubmit.mock.calls[0][0].results[0].customInput).toBe("custom detail");
+		expect(onSubmit.mock.calls[0][0].results[0]).toMatchObject({
+			selectedOptions: ["option b"],
+			customInput: undefined,
+		});
 	});
 
-	it("defers a timeout that fires during a pending prompt and honors the resolved custom input", async () => {
+	it("defers a timeout while the external editor is open", async () => {
 		vi.useFakeTimers();
 		const deferred = Promise.withResolvers<string | undefined>();
-		const onPrompt = vi.fn().mockReturnValue(deferred.promise);
+		const onEditInput = vi.fn().mockReturnValue(deferred.promise);
 		const onSubmit = vi.fn();
 		const onTimeout = vi.fn();
 		const questions: ExtensionAskDialogQuestion[] = [
 			{
 				id: "q1",
-				question: "First?",
-				options: [{ label: "Option A" }, { label: "Option B" }],
+				question: "first?",
+				options: [{ label: "option a" }, { label: "option b" }],
 			},
 			{
 				id: "q2",
-				question: "Second?",
-				options: [{ label: "Option C" }, { label: "Option D" }],
+				question: "second?",
+				options: [{ label: "option c" }, { label: "option d" }],
 				recommended: 1,
 			},
 		];
-
 		const component = new AskDialogComponent(
 			questions,
-			{ onSubmit, onCancel: vi.fn(), onPrompt },
+			{ onSubmit, onCancel: vi.fn(), onPrompt: vi.fn(), onEditInput },
 			{ timeout: 1000, onTimeout },
 		);
 
-		// Open the "Other (type your own)" prompt on question 1.
 		component.handleInput(DOWN);
 		component.handleInput(DOWN);
-		component.handleInput(ENTER);
-		expect(onPrompt).toHaveBeenCalledTimes(1);
-
-		// Timer expires while the prompt is pending: the timeout must be deferred,
-		// not submit the recommended fallback out from under the user.
+		component.handleInput("my answer");
+		component.handleInput(ctrlG);
+		expect(onEditInput).toHaveBeenCalledWith("my answer");
 		vi.advanceTimersByTime(1000);
 		expect(onTimeout).not.toHaveBeenCalled();
 		expect(onSubmit).not.toHaveBeenCalled();
 
-		// Resolving the prompt honors the typed answer, then runs the deferred
-		// timeout handling exactly once.
 		deferred.resolve("my answer");
 		await Promise.resolve();
 		await Promise.resolve();
@@ -646,36 +765,26 @@ describe("AskDialogComponent", () => {
 		expect(results[0].customInput).toBe("my answer");
 		expect(results[0].selectedOptions).toEqual([]);
 		expect(results[0].timedOut).toBeUndefined();
-		expect(results[1].selectedOptions).toEqual(["Option D"]);
+		expect(results[1].selectedOptions).toEqual(["option d"]);
 		expect(results[1].timedOut).toBe(true);
 	});
 
-	it("keeps a single-question custom prompt answer when timeout expires while the prompt is pending", async () => {
+	it("keeps one custom answer when timeout expires during external editing", async () => {
 		vi.useFakeTimers();
 		const deferred = Promise.withResolvers<string | undefined>();
-		const onPrompt = vi.fn().mockReturnValue(deferred.promise);
+		const onEditInput = vi.fn().mockReturnValue(deferred.promise);
 		const onSubmit = vi.fn();
 		const onTimeout = vi.fn();
-		const questions: ExtensionAskDialogQuestion[] = [
-			{
-				id: "q1",
-				question: "Only question?",
-				options: [{ label: "Fallback" }],
-			},
-		];
-
 		const component = new AskDialogComponent(
-			questions,
-			{ onSubmit, onCancel: vi.fn(), onPrompt },
+			[{ id: "q1", question: "only question?", options: [{ label: "fallback" }] }],
+			{ onSubmit, onCancel: vi.fn(), onPrompt: vi.fn(), onEditInput },
 			{ timeout: 1000, onTimeout },
 		);
 
 		component.handleInput(DOWN);
-		component.handleInput(ENTER);
-		expect(onPrompt).toHaveBeenCalledTimes(1);
-
+		component.handleInput("my answer");
+		component.handleInput(ctrlG);
 		vi.advanceTimersByTime(1000);
-		expect(onTimeout).not.toHaveBeenCalled();
 		expect(onSubmit).not.toHaveBeenCalled();
 
 		deferred.resolve("my answer");
@@ -683,7 +792,6 @@ describe("AskDialogComponent", () => {
 		await Promise.resolve();
 
 		expect(onSubmit).toHaveBeenCalledTimes(1);
-		expect(onTimeout).not.toHaveBeenCalled();
 		const result = onSubmit.mock.calls[0][0].results[0];
 		expect(result.customInput).toBe("my answer");
 		expect(result.selectedOptions).toEqual([]);
@@ -801,77 +909,26 @@ describe("AskDialogComponent", () => {
 		expect(onTimeout).toHaveBeenCalledTimes(1);
 	});
 
-	it("does not reset the countdown while a prompt is active", async () => {
+	it("does not reset the countdown while the external editor is open", async () => {
 		vi.useFakeTimers();
 		const deferred = Promise.withResolvers<string | undefined>();
-		const onPrompt = vi.fn().mockReturnValue(deferred.promise);
+		const onEditInput = vi.fn().mockReturnValue(deferred.promise);
 		const onTimeout = vi.fn();
-		const questions: ExtensionAskDialogQuestion[] = [
-			{
-				id: "q1",
-				question: "Choose one?",
-				options: [{ label: "Option A" }],
-			},
-		];
-
 		const component = new AskDialogComponent(
-			questions,
-			{ onSubmit: vi.fn(), onCancel: vi.fn(), onPrompt },
+			[{ id: "q1", question: "choose one?", options: [{ label: "option a" }] }],
+			{ onSubmit: vi.fn(), onCancel: vi.fn(), onPrompt: vi.fn(), onEditInput },
 			{ timeout: 5000, onTimeout },
 		);
 
-		// Open the custom-input prompt (DOWN to "Other", ENTER).
-		component.handleInput(DOWN);
-		component.handleInput(ENTER);
-		expect(onPrompt).toHaveBeenCalledTimes(1);
-
-		// While the prompt is pending, input is guarded — no reset.
 		component.handleInput(DOWN);
+		component.handleInput(ctrlG);
 		vi.advanceTimersByTime(5000);
-		// Timeout is deferred during prompt, not fired.
 		expect(onTimeout).not.toHaveBeenCalled();
-
 		deferred.resolve("answer");
 		await Promise.resolve();
 		await Promise.resolve();
 	});
 
-	it("bounds custom input prompt title for long multi-line questions", async () => {
-		const onPrompt = vi.fn().mockReturnValue(Promise.resolve("custom"));
-		const longQuestion = "This is a very long question ".repeat(20);
-		const questions: ExtensionAskDialogQuestion[] = [
-			{
-				id: "q1",
-				question: longQuestion,
-				options: [{ label: "Option A" }],
-			},
-		];
-
-		const component = new AskDialogComponent(questions, {
-			onSubmit: vi.fn(),
-			onCancel: vi.fn(),
-			onPrompt,
-		});
-
-		// Navigate to "Other" and press Enter to trigger the custom prompt.
-		component.handleInput(DOWN);
-		component.handleInput(ENTER);
-		await Promise.resolve();
-		await Promise.resolve();
-
-		expect(onPrompt).toHaveBeenCalledTimes(1);
-		const title = onPrompt.mock.calls[0][0] as string;
-		const lines = title.split("\n");
-		// Title must be bounded to at most MAX_PROMPT_TITLE_ROWS lines.
-		expect(lines.length).toBeLessThanOrEqual(3);
-		// Each line must fit within the terminal content width.
-		for (const line of lines) {
-			expect(stripVTControlCharacters(line).length).toBeLessThanOrEqual((process.stdout.columns ?? 80) - 4);
-		}
-		// Must contain the prefix and a truncation indicator on the last line.
-		expect(stripVTControlCharacters(title)).toContain("Custom answer:");
-	});
-
 	it("bounds note prompt title for long multi-line questions", async () => {
 		const onPrompt = vi.fn().mockReturnValue(Promise.resolve("note"));
 		const longQuestion = "Multi\nline\nquestion ".repeat(30);
@@ -1087,7 +1144,7 @@ describe("AskDialogComponent", () => {
 			const out = render(component);
 
 			expect(out).toContain("PgUp/PgDn");
-			expect(out).toContain("Tab/←/→");
+			expect(out).toContain("Tab");
 			expect(out).not.toContain(" tabs");
 			expect(out).toContain("Ctrl+G cancel");
 			setKeybindings(
@@ -1141,8 +1198,8 @@ describe("AskDialogComponent", () => {
 			expect(out).toContain("PgUp/PgDn");
 			expect(out).toContain("PREVIEW-MIDDLE");
 			component.handleInput(DOWN);
-			out = render(component);
-			expect(out).toContain("Other (type your own)");
+			out = component.render(80).join("\n");
+			expect(out).toContain("Shift+Enter new line");
 			expect(out).not.toContain("PREVIEW-MIDDLE");
 			expect(out).not.toContain("PgUp/PgDn");
 			component.handleInput(UP);
@@ -1154,10 +1211,10 @@ describe("AskDialogComponent", () => {
 				out = render(component);
 			}
 			expect(out).toContain("PREVIEW-LAST");
-			expect(out).not.toContain("Other (type your own)");
+			expect(out).not.toContain("Shift+Enter new line");
 			component.handleInput(DOWN);
-			out = render(component);
-			expect(out).toContain("Other (type your own)");
+			out = component.render(80).join("\n");
+			expect(out).toContain("Shift+Enter new line");
 			component.handleInput(UP);
 			out = render(component);
 			for (let page = 0; page < 10; page++) {
@@ -1250,44 +1307,34 @@ describe("AskDialogComponent", () => {
 		expect(matches?.length ?? 0).toBeLessThan(10);
 	});
 
-	it("Other editor cancel returns to the option list without submitting", async () => {
-		const onPrompt = vi.fn().mockReturnValue(Promise.resolve(undefined));
+	it("external editor cancellation returns to the option list", async () => {
+		const onEditInput = vi.fn().mockReturnValue(Promise.resolve(undefined));
 		const onSubmit = vi.fn();
 		const onCancel = vi.fn();
-		const questions: ExtensionAskDialogQuestion[] = [
-			{
-				id: "q1",
-				question: "Choose one?",
-				options: [{ label: "Option A" }, { label: "Option B" }],
-			},
-		];
-
-		const component = new AskDialogComponent(questions, {
-			onSubmit,
-			onCancel,
-			onPrompt,
-		});
+		const component = new AskDialogComponent(
+			[
+				{
+					id: "q1",
+					question: "choose one?",
+					options: [{ label: "option a" }, { label: "option b" }],
+				},
+			],
+			{ onSubmit, onCancel, onPrompt: vi.fn(), onEditInput },
+		);
 
-		// Navigate to "Other" and press Enter to open the custom input prompt.
 		component.handleInput(DOWN);
 		component.handleInput(DOWN);
-		component.handleInput(ENTER);
+		component.handleInput(ctrlG);
 		await Promise.resolve();
 		await Promise.resolve();
-
-		// The prompt was cancelled (returns undefined). The dialog must stay
-		// open — no submit, no cancel.
-		expect(onPrompt).toHaveBeenCalledTimes(1);
+		expect(onEditInput).toHaveBeenCalledTimes(1);
 		expect(onSubmit).not.toHaveBeenCalled();
 		expect(onCancel).not.toHaveBeenCalled();
 
-		// The dialog should still be usable: select Option A and submit.
-		component.handleInput("\x1b[A"); // UP to Option B
-		component.handleInput("\x1b[A"); // UP to Option A
+		component.handleInput(UP);
+		component.handleInput(UP);
 		component.handleInput(ENTER);
-
-		expect(onSubmit).toHaveBeenCalledTimes(1);
-		expect(onSubmit.mock.calls[0][0].results[0].selectedOptions).toEqual(["Option A"]);
+		expect(onSubmit.mock.calls[0][0].results[0].selectedOptions).toEqual(["option a"]);
 	});
 
 	it("keeps a fixed spawn-time height across tabs, clamped to 70% of the terminal", () => {
@@ -1321,46 +1368,31 @@ describe("AskDialogComponent", () => {
 		expect(component.render(80).length).toBe(questionTab.length);
 	});
 
-	it("clears the custom answer when the Other prompt is submitted empty", async () => {
-		const onPrompt = vi.fn();
+	it("clears an inline custom draft with ctrl+u", () => {
 		const onSubmit = vi.fn();
-		const questions: ExtensionAskDialogQuestion[] = [
-			{
-				id: "q1",
-				question: "Choose multiple?",
-				options: [{ label: "Option A" }, { label: "Option B" }],
-				multi: true,
-			},
-		];
-		const component = new AskDialogComponent(questions, {
-			onSubmit,
-			onCancel: vi.fn(),
-			onPrompt,
-		});
+		const component = new AskDialogComponent(
+			[
+				{
+					id: "q1",
+					question: "choose multiple?",
+					options: [{ label: "option a" }, { label: "option b" }],
+					multi: true,
+				},
+			],
+			{ onSubmit, onCancel: vi.fn(), onPrompt: vi.fn() },
+		);
 
-		// Set a custom answer via Other.
-		onPrompt.mockReturnValueOnce(Promise.resolve("my custom answer"));
-		component.handleInput(DOWN); // Option B
-		component.handleInput(DOWN); // Other
-		component.handleInput(ENTER);
-		await Promise.resolve();
-		await Promise.resolve();
+		component.handleInput(DOWN);
+		component.handleInput(DOWN);
+		component.handleInput("my custom answer");
 		expect(render(component)).toContain("my custom answer");
-
-		// Reopen Other (prefilled with the current answer) and submit an
-		// empty value: the custom answer is unselected.
-		onPrompt.mockReturnValueOnce(Promise.resolve(""));
-		component.handleInput(ENTER);
-		await Promise.resolve();
-		await Promise.resolve();
-		expect(onPrompt).toHaveBeenNthCalledWith(2, expect.any(String), "my custom answer");
+		component.handleInput("\x15");
 		expect(render(component)).not.toContain("my custom answer");
-
-		// Submitting confirms nothing was kept.
-		component.handleInput(TAB);
 		component.handleInput(ENTER);
-		expect(onSubmit).toHaveBeenCalledTimes(1);
-		expect(onSubmit.mock.calls[0][0].results[0].customInput).toBeUndefined();
+		expect(onSubmit.mock.calls[0][0].results[0]).toMatchObject({
+			selectedOptions: [],
+			customInput: "",
+		});
 	});
 
 	it("normalizes malformed questions so render and submit do not crash", () => {
diff --git a/packages/coding-agent/test/tools/ask.test.ts b/packages/coding-agent/test/tools/ask.test.ts
index 6a5688d268..cc4ad1c6bf 100644
--- a/packages/coding-agent/test/tools/ask.test.ts
+++ b/packages/coding-agent/test/tools/ask.test.ts
@@ -82,6 +82,15 @@ beforeAll(async () => {
 	await initTheme(false);
 });
 
+describe("ask tool guidance", () => {
+	it("requires one questionnaire before new-project work", () => {
+		const tool = new AskTool(createSession());
+		expect(tool.description).toContain(
+			"the user starts a new project, application, service, library, or website. ask one batched questionnaire before planning or editing.",
+		);
+	});
+});
+
 describe("AskTool cancellation", () => {
 	it("aborts the turn when the user cancels selection", async () => {
 		const tool = new AskTool(createSession());
@@ -290,7 +299,7 @@ describe("AskTool cancellation", () => {
 				return "custom response";
 			},
 		);
-		const select = vi.fn(async () => "Other (type your own)");
+		const select = vi.fn(async () => "Type something.");
 		const context = createContext({
 			select,
 			editor,
@@ -338,7 +347,7 @@ describe("AskTool cancellation", () => {
 				const timeout = dialogOptions?.timeout ?? 1;
 				await Bun.sleep(timeout + 5);
 				dialogOptions?.onTimeout?.();
-				return "Other (type your own)";
+				return "Type something.";
 			},
 			editor,
 			abort,
@@ -557,7 +566,7 @@ describe("AskTool custom input", () => {
 			},
 		];
 		const context = createContext({
-			select: async () => "Other (type your own)",
+			select: async () => "Type something.",
 			editor,
 			abort,
 		});
@@ -584,7 +593,7 @@ describe("AskTool custom input", () => {
 			},
 		];
 		const context = createContext({
-			select: async () => "Other (type your own)",
+			select: async () => "Type something.",
 			editor,
 		});
 
@@ -595,7 +604,7 @@ describe("AskTool custom input", () => {
 		expect(title).toContain("yes");
 		expect(title).toContain("no");
 		expect(title).toContain("Skip the optional detail.");
-		expect(title).toContain("Other (type your own)");
+		expect(title).toContain("Type something.");
 		expect(title).toContain("Enter your response:");
 	});
 
@@ -610,7 +619,7 @@ describe("AskTool custom input", () => {
 		}));
 		const questions = [{ id: "pick", question: "Pick one", options }];
 		const context = createContext({
-			select: async () => "Other (type your own)",
+			select: async () => "Type something.",
 			editor,
 		});
 
@@ -623,7 +632,7 @@ describe("AskTool custom input", () => {
 		expect(lineCount).toBeLessThanOrEqual(22);
 		expect(title).toContain("Pick one");
 		expect(title).toContain("option-0");
-		expect(title).toContain("Other (type your own)");
+		expect(title).toContain("Type something.");
 		expect(title).toContain("more option");
 		expect(title).toContain("Enter your response:");
 		// Descriptions are flattened to a single line and truncated.
@@ -645,7 +654,7 @@ describe("AskTool custom input", () => {
 				call += 1;
 				if (call === 1) return selectItemLabel(opts.find(o => selectItemLabel(o) === "opt-12"));
 				if (call === 2) return selectItemLabel(opts.find(o => selectItemLabel(o) === "opt-17"));
-				return "Other (type your own)";
+				return "Type something.";
 			},
 			editor,
 		});
@@ -657,7 +666,7 @@ describe("AskTool custom input", () => {
 		// already toggled before switching to Other.
 		expect(title).toContain("opt-12");
 		expect(title).toContain("opt-17");
-		expect(title).toContain("Other (type your own)");
+		expect(title).toContain("Type something.");
 		expect(title).toContain("more option");
 	});
 
@@ -674,7 +683,7 @@ describe("AskTool custom input", () => {
 					call += 1;
 					return selectItemLabel(opts.find(o => selectItemLabel(o) === label));
 				}
-				return "Other (type your own)";
+				return "Type something.";
 			},
 			editor,
 		});
@@ -684,9 +693,9 @@ describe("AskTool custom input", () => {
 		const title = editor.mock.calls[0]?.[0] ?? "";
 		const optionRows = title
 			.split("\n")
-			.filter(line => line.includes("checked-") || line.includes("Other (type your own)"));
+			.filter(line => line.includes("checked-") || line.includes("Type something."));
 		expect(optionRows.length).toBeLessThanOrEqual(8);
-		expect(title).toContain("Other (type your own)");
+		expect(title).toContain("Type something.");
 		expect(title).toContain("checked");
 		expect(title).toContain("more option");
 		expect(title).toContain("Enter your response:");
@@ -702,7 +711,7 @@ describe("AskTool custom input", () => {
 		const context = createContext({
 			select: async (_prompt, opts) => {
 				const next = checkedLabels[call++];
-				return next ? selectItemLabel(opts.find(o => selectItemLabel(o) === next)) : "Other (type your own)";
+				return next ? selectItemLabel(opts.find(o => selectItemLabel(o) === next)) : "Type something.";
 			},
 			editor,
 		});
@@ -711,7 +720,7 @@ describe("AskTool custom input", () => {
 
 		const title = editor.mock.calls[0]?.[0] ?? "";
 		expect(title.split("\n").length).toBeLessThanOrEqual(16);
-		expect(title).toContain("Other (type your own)");
+		expect(title).toContain("Type something.");
 		expect(title).toContain("more option");
 		expect(title).toContain("Enter your response:");
 	});
@@ -731,7 +740,7 @@ describe("AskTool custom input", () => {
 			}));
 			const questions = [{ id: "pick", question: "Pick one", options }];
 			const context = createContext({
-				select: async () => "Other (type your own)",
+				select: async () => "Type something.",
 				editor,
 			});
 
@@ -746,7 +755,7 @@ describe("AskTool custom input", () => {
 				expect(stripAnsi(line).length).toBeLessThanOrEqual(80);
 			}
 			expect(title).toContain("Pick one");
-			expect(title).toContain("Other (type your own)");
+			expect(title).toContain("Type something.");
 			expect(title).toContain("Enter your response:");
 			expect(title).not.toContain("x".repeat(400));
 		} finally {
@@ -773,7 +782,7 @@ describe("AskTool custom input", () => {
 		const context = createContext({
 			select: async () => {
 				selectCalls += 1;
-				return selectCalls === 1 ? "Other (type your own)" : "yes";
+				return selectCalls === 1 ? "Type something." : "yes";
 			},
 			editor,
 			abort,
@@ -809,7 +818,7 @@ describe("AskTool custom input", () => {
 				if (prompt.includes("First?")) return "one";
 				if (prompt.includes("Details?")) {
 					detailsVisits += 1;
-					return detailsVisits === 1 ? "Other (type your own)" : "short";
+					return detailsVisits === 1 ? "Type something." : "short";
 				}
 				return undefined;
 			},
@@ -848,7 +857,7 @@ describe("AskTool custom input", () => {
 			},
 		];
 		const context = createContext({
-			select: async () => "Other (type your own)",
+			select: async () => "Type something.",
 			editor,
 			abort,
 		});
@@ -865,7 +874,7 @@ describe("AskTool custom input", () => {
 		const abort = vi.fn();
 		const editor = vi.fn(async () => "");
 		const context = createContext({
-			select: async () => "Other (type your own)",
+			select: async () => "Type something.",
 			editor,
 			abort,
 		});
@@ -909,7 +918,7 @@ describe("AskTool custom input", () => {
 					if (!alphaOption) throw new Error("Missing alpha option");
 					return selectItemLabel(alphaOption);
 				}
-				return "Other (type your own)";
+				return "Type something.";
 			},
 			editor,
 		});
@@ -962,7 +971,7 @@ describe("AskTool custom input", () => {
 				}
 				if (step === 1) {
 					step += 1;
-					return "Other (type your own)";
+					return "Type something.";
 				}
 				const doneOption = options.find(option => selectItemLabel(option)?.includes("Done selecting"));
 				if (!doneOption) throw new Error("Missing done option");
@@ -1006,7 +1015,7 @@ describe("AskTool multiline custom input rendering", () => {
 		const multilineText = "first line\nsecond line\nthird line";
 		const editor = vi.fn(async () => multilineText);
 		const context = createContext({
-			select: async () => "Other (type your own)",
+			select: async () => "Type something.",
 			editor,
 		});
 
@@ -1063,7 +1072,7 @@ describe("AskTool multiline custom input rendering", () => {
 		const multilineText = "\nsecond line";
 		const editor = vi.fn(async () => multilineText);
 		const context = createContext({
-			select: async () => "Other (type your own)",
+			select: async () => "Type something.",
 			editor,
 		});
 
@@ -1262,7 +1271,7 @@ describe("AskTool multi-question navigation", () => {
 			select: async (prompt, _options, dialogOptions) => {
 				if (prompt.includes("Details?")) {
 					detailVisits += 1;
-					if (detailVisits === 1) return "Other (type your own)";
+					if (detailVisits === 1) return "Type something.";
 					dialogOptions?.onRight?.();
 					return undefined;
 				}
@@ -1308,7 +1317,7 @@ describe("AskTool multi-question navigation", () => {
 					detailVisits += 1;
 					if (detailVisits === 1) return "short";
 					// Second visit: try Other then dismiss editor, then forward
-					if (detailVisits === 2) return "Other (type your own)";
+					if (detailVisits === 2) return "Type something.";
 					dialogOptions?.onRight?.();
 					return undefined;
 				}
@@ -1665,7 +1674,7 @@ describe("AskTool rich ask dialog", () => {
 
 		expect(select).toHaveBeenCalledTimes(1);
 		// verify preview/header are NOT forwarded to select options
-		expect(select.mock.calls[0][1]).toEqual([{ label: "Option A", description: "Desc A" }, "Other (type your own)"]);
+		expect(select.mock.calls[0][1]).toEqual([{ label: "Option A", description: "Desc A" }, "Type something."]);
 	});
 
 	it("rejects reserved-label collision in parameters validation", async () => {
@@ -1677,7 +1686,7 @@ describe("AskTool rich ask dialog", () => {
 		expect(valid instanceof type.errors).toBe(false);
 
 		const reservedOther = tool.parameters({
-			questions: [{ id: "q1", question: "Q?", options: [{ label: "Other (type your own)" }] }],
+			questions: [{ id: "q1", question: "Q?", options: [{ label: "Type something." }] }],
 		});
 		expect(reservedOther instanceof type.errors).toBe(true);
 
-- 
2.43.0

